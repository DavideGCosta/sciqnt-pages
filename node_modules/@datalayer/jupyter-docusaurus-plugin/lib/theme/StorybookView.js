"use strict";
/*
 * Copyright (c) 2021-2023 Datalayer, Inc.
 *
 * MIT License
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.StorybookView = void 0;
const tslib_1 = require("tslib");
const react_1 = tslib_1.__importDefault(require("react"));
const FRAME_STYLE = {
    border: '1px solid #aaa',
};
/**
 * Embeds a Storybook example
 *
 * @param story The story ID to display
 * @param args The story arguments
 */
function StorybookView({ story = '', args }) {
    let queryArgs = '';
    if (args) {
        const params = Object.entries(args).map(([k, v]) => `${encodeURIComponent(k)}:${encodeURIComponent(v)}`).join(';');
        if (params) {
            queryArgs = `&args=${params}`;
        }
    }
    return (react_1.default.createElement("iframe", { title: "Jupyter React Storybook Component", src: `https://jupyter-ui-storybook.datalayer.tech/iframe.html?id=${story}&viewMode=story${queryArgs}`, width: "100%", height: "600", style: FRAME_STYLE, allow: 'clipboard-write;' }));
}
exports.StorybookView = StorybookView;
