import { IOutput } from '@jupyterlab/nbformat';
import { IOutputAreaModel, OutputArea } from '@jupyterlab/outputarea';
import Kernel from '../../jupyter/kernel/Kernel';
export declare class OutputAdapter {
    private _kernel?;
    private _renderers;
    private _outputArea;
    private _rendermime;
    private _iPyWidgetsClassicManager;
    constructor(kernel?: Kernel, outputs?: IOutput[], outputAreaModel?: IOutputAreaModel);
    execute(code: string): Promise<void>;
    interrupt(): void;
    clear(): void;
    get kernel(): Kernel | undefined;
    set kernel(kernel: Kernel | undefined);
    get outputArea(): OutputArea;
    private initKernel;
}
export default OutputAdapter;
