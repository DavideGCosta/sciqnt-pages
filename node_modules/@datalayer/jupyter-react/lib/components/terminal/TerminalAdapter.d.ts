import { BoxPanel } from '@lumino/widgets';
import { ServerConnection } from '@jupyterlab/services';
import { ITerminal } from '@jupyterlab/terminal';
import { Terminal } from './Terminal';
export declare class TerminalAdapter {
    private terminal;
    private terminalPanel;
    constructor(options: TerminalAdapter.ITerminalAdapterOptions);
    get panel(): BoxPanel;
    setTheme(theme: ITerminal.Theme): void;
}
export declare namespace TerminalAdapter {
    interface ITerminalAdapterOptions extends Terminal.ITerminalOptions {
        serverSettings: ServerConnection.ISettings;
    }
}
export default TerminalAdapter;
